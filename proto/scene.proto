syntax = "proto3";

message Scene {
    message Cycle {
        uint32 flags = 1;
        uint32 rate = 2;
        uint32 low = 3;
        uint32 high = 4;
    }
    uint32 width = 1;
    uint32 height = 2;
    bytes pixels = 3;
    repeated uint32 palette = 4;
    repeated Cycle cycles = 5;
}
